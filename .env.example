# ==============================================================================
# 🏙️ IluminaCity - Variáveis de Ambiente (Exemplo)
# Crie um arquivo .env na raiz do projeto e copie este conteúdo.
# NUNCA comite o arquivo .env real com senhas de produção!
# ==============================================================================

# ------------------------------------------------------------------------------
# 🐳 DATABASE & DOCKER INFRASTRUCTURE
# Configurações usadas pelo docker-compose.yml para subir o banco Postgres
# ------------------------------------------------------------------------------
DB_USER=admin
DB_PASSWORD=admin
DB_NAME=iluminacity_db
DB_PORT=5432

# Porta onde o site (frontend) será exposto no host
WEB_PORT=8080


# ------------------------------------------------------------------------------
# 🚀 BACKEND (API - Fastify)
# Configurações para a API rodando dentro do container ou localmente
# ------------------------------------------------------------------------------

# URL de Conexão com o Banco de Dados (PostgreSQL)
# formato: postgresql://USER:PASSWORD@HOST:PORT/DB_NAME
#
# -> EM PRODUÇÃO (Docker): O host deve ser 'db' (nome do serviço no compose)
DATABASE_URL=postgresql://admin:admin@db:5432/iluminacity_db
#
# -> EM DESENVOLVIMENTO (Local): O host geralmente é 'localhost' (se rodar fora do docker)
# DATABASE_URL=postgresql://admin:admin@localhost:5435/iluminacity_db

# Segredo para assinar os tokens JWT de autenticação
# CRÍTICO: Em produção, gere uma string aleatória longa e segura!
JWT_SECRET=desenvolvimento_apenas_troque_isso_em_producao_por_hash_seguro

# Porta interna da API (Padrão: 3333)
PORT=3333


# ------------------------------------------------------------------------------
# 🌐 FRONTEND (Web - Vite/React)
# Variáveis injetadas no build do frontend
# ------------------------------------------------------------------------------

# URL base da API
# Se vazio, assume '/api' (o que é ideal quando usado com Nginx Reverse Proxy)
# VITE_API_URL=http://localhost:3333
